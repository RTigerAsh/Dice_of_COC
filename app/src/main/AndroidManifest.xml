<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cn.edu.swufe.dice_of_coc">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_main"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--android:roundIcon="@mipmap/ic_launcher_round"-->
        <activity android:name=".BuildCardActivity">

        </activity>
        <activity android:name=".MainSkillsActivity" />
        <activity android:name=".MonsterActivity" />
        <activity android:name=".WeaponListActivity" />
        <activity android:name=".RuleBookActivity" />
        <activity android:name=".WelcomeGuideActivity" />
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/colortext" />

        <activity android:name=".MainActivity"></activity>
    </application>

</manifest>